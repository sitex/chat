# Stack

- Python package built with setuptools; runtime floor Python 3.10 (`pyproject.toml`).
- Main dependencies: python-telegram-bot, Anthropic, httpx, aiohttp, and python-dotenv (`pyproject.toml`).
- pytest, Ruff, and mypy are configured through `pyproject.toml` and `Makefile`.
